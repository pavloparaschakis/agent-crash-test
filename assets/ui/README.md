# Report UI assets

The HTML reporter intentionally embeds its CSS and JavaScript so every generated
report is a portable, offline artifact. This directory is reserved for future
screenshots and design-source assets; the local browser does not fetch assets,
fonts, telemetry, or code from the network.
