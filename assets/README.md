# Launch assets

- [`demo-terminal.gif`](demo-terminal.gif) is a generated animated terminal explainer for the verified timeout → retry → duplicate-effect scenario.
- [`state-diff.png`](state-diff.png) is a generated before/after effect-contract illustration.
- The GIF is a launch illustration, not evidence of a hosted GitHub run. The final launch still needs a real screen recording from the public repository and Action workflow.
